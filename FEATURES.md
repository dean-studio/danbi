# 단비 — 차별화 기능

> **한 줄 요약.** 단비는 AI와 함께 여러 프로젝트를 동시에 굴리는 한국 개발자를 위한 로컬 마크다운 에이전트 앱입니다. 노션은 무겁고, Obsidian은 비어있고, Raycast는 맥락이 없습니다. 단비는 세 가지를 동시에 해결합니다.

## 목차

1. [설계 철학](#1-설계-철학)
2. [타깃 사용자](#2-타깃-사용자)
3. [차별화 기능 7가지](#3-차별화-기능-7가지)
4. [경쟁 도구와의 비교](#4-경쟁-도구와의-비교)
5. [카파시 LLM Wiki 방법론과의 관계](#5-카파시-llm-wiki-방법론과의-관계)
6. [비용 모델](#6-비용-모델)

---

## 1. 설계 철학

### 주어를 바꾼다
- **노션·Obsidian**: *사람이* 노트를 쓰고 정리한다. AI는 옵션.
- **단비**: *에이전트가* 파일을 관리한다. 사람은 **말로 지시**할 뿐.

이 전환이 단비의 모든 UX 결정의 뿌리입니다. 에디터보다 **채팅 입력창**이 일등 시민이고, 프로젝트 폴더 구조보다 **라우팅 스키마**가 우선합니다.

### 구조 강제 + 자동 라우팅
- `config.json`에 등록된 **프로젝트 × 도메인 격자** 안에서만 AI가 쓰기
- 사용자가 "어디에 뭘 넣을지" 고민하지 않음 — 라우터 LLM이 판단
- "나중에 정리해야지" 부채가 생기지 않도록 처음부터 자리를 정함

### 로컬 우선, 데이터 주권
- 모든 파일 = 사용자 기기의 `~/Danbi_Vault` 안 마크다운
- Vault 자체가 git repo → 모든 AI 수정이 자동 커밋
- API 키는 macOS 키체인에만. 단비 서버 없음
- **BYOK (Bring Your Own Key)** — AWS Bedrock 직접 호출

---

## 2. 타깃 사용자

### 핵심 페르소나
**이진호 · 33세 · AI 프로덕트 엔지니어**

> 회사 업무 하나 + 사이드 프로젝트 3개를 동시에 굴린다. Claude Code / Cursor / 터미널을 띄워 놓고 하루에 4번 컨텍스트를 바꾼다. 회의 도중 "이건 나중에 잊어버리지 말자" 생각나면 Apple Notes에 휘갈겨 놓지만 **3일 지나면 어디 적었는지 모른다**. Notion은 열 때마다 무겁고, Obsidian을 깔았지만 **폴더 구조 짜다가 포기**했다.

이 사람의 진짜 고통:
- **메모의 격리가 불가능** — Apple Notes는 평면, 어디서 찾을지 모름
- **컨텍스트 스위칭 비용** — Cursor에서 빠져나와 노트 앱 열고 붙여넣는 과정 자체가 흐름을 끊음
- **축적의 실패** — 작성했던 결정/스펙을 다음 번에 검색해도 안 나옴

이게 단비가 풀려는 문제.

### 2차 페르소나
- 사이드 프로젝트 여러 개 운영하는 인디해커
- 논문·책 많이 읽으며 노트하는 대학원생 (Vault 템플릿 덕분)
- 개발팀 리더가 팀 내 지식 베이스를 개인 수준에서 관리

---

## 3. 차별화 기능 7가지

각 기능은 **구체적 사용자 시나리오 + 구현 방식 + 경쟁 도구 대비 뾰족한 지점**으로 설명합니다.

### 3.1. 🪄 Quick Capture (전역 핫키 + 백그라운드)

**시나리오**
> Cursor에서 코딩하다가 "로그인 토큰 만료가 24시간이면 너무 짧은가?" 궁금해짐. `⌃Space` 한 번 → "보니 로그인 토큰 24h vs 7d 고민. JWT + refresh token 고려" 타이핑 후 Enter. 팝업이 **즉시 사라지고** 코딩 계속. 3초 뒤 "단비 · 보니/backend.md" 시스템 알림. 에이전트가 `보니/backend.md`에 알아서 append.

**기술**
- Tauri global shortcut (기본 `⌃Space`, 설정에서 변경 가능)
- Enter → UI 즉시 hide + 백그라운드에서 Haiku 라우팅 + Writer 플래닝 + 자동 커밋
- macOS 트레이바 아이콘 상주 — 앱 창 닫혀도 동작
- 한 번 사용한 프로젝트는 "sticky chip"으로 기억 → 다음 캡처에 미리 박혀 있음
- 입력 중 프로젝트명 매칭되면 `Tab`으로 자동 칩 고정

**왜 Alfred/Raycast가 못하는가**
- Alfred는 런처 + 단순 AI. 내 vault 구조를 모름.
- Raycast AI는 클립보드만 다룸. 파일을 안 건드림.
- 단비는 **vault 스키마를 라우터에 주입**해서 "보니 프로젝트의 backend.md"까지 파일 경로 단위로 판단.

### 3.2. 🔍 3단계 검색 (비용 0원 우선)

**시나리오**
> "Refresh token을 얼마나 길게 하기로 결정했더라?" `⌘K` → "refresh token" 타이핑. 0.08초 만에 결과 4개가 뜸. 그 중 `보니/backend.md` 클릭하면 해당 섹션으로 이동.

**기술**
- **Tier 1 (substring, 0ms)**: 파일명 · 헤딩 · 프리뷰 즉시 매치
- **Tier 2 (tantivy BM25 + ngram, 150ms)**: 전체 본문 + 한국어 substring 정확도
- **Tier 3 (Haiku 재랭킹, 버튼 클릭 시)**: 자연어 쿼리("인증 어떻게 처리하지?") → 의미 기반 정렬
- Tier 1/2 완전 무료, Tier 3만 사용자가 명시적으로 요청할 때 Haiku 호출

**왜 Obsidian이 못하는가**
- Obsidian 기본 검색은 정규식 기반 — 한국어 형태소 약함
- Smart Connections 같은 플러그인은 전체 embedding 계산, 매 쿼리마다 LLM 호출 → 비용·속도 모두 불리
- 단비는 **공짜 검색이 기본**, AI는 진짜 필요할 때만

### 3.3. 📎 Document Ingestion (PDF·DOCX를 프로젝트 지식으로)

**시나리오**
> 회의 후 PDF 회의록 3개를 받음. 채팅창에 드래그 앤 드롭 → "이거 다 읽고 주요 결정사항만 `/보니/plan.md`에 정리해줘" → Writer가 요약해서 적절한 섹션에 append.

**기술**
- Rust `pdf-extract` + `docx-rs`로 로컬 파싱 (클라우드 업로드 X)
- 첨부 본문이 라우팅/플래닝 컨텍스트로 들어감
- 20만 자 상한 (~50k 토큰) 자동 클리핑 — 너무 긴 PDF는 Haiku로 선제 요약 단계 (미래)

**왜 Notion이 못하는가**
- Notion에 PDF 드롭 = 단지 파일 블록 생성. 내용은 수동 복붙.
- 단비는 **구조화된 도메인에 내용 분산**까지 한 번에.

### 3.4. 🧵 Compound (흩어진 지식 재구성)

**시나리오**
> 3개월 동안 `보니/backend.md`, `보니/plan.md`, `보니/ui.md`에 인증 관련 메모가 조각조각 쌓임. 채팅에 "auth 관련 내용 모아서 `/보니/concepts/auth.md` 만들어줘" → 단비가 tantivy로 관련 문서 top 12개 찾고 → Writer가 섹션별로 재구성한 새 마크다운 생성 → 미리보기 확인 후 "합성하기" 클릭하면 저장.

**기술**
- 라우터가 `intent=compound` 판단
- Rust가 tantivy로 소스 상위 문서 수집 (max 12개, 각 12,000자 상한)
- Writer가 새 파일 전체 본문 생성 + 어떤 소스를 썼는지 `[[project/domain.md]]` wiki 링크로 인용
- 미리보기 카드 + 예상 토큰 비용 표시 → 사용자 승인 버튼으로 실제 저장

**왜 이게 단비만 가능한가**
- Obsidian AI 플러그인은 현재 페이지 기반 — **vault 전체 교차 분석 없음**
- 노션 AI는 한 페이지 쓰기·요약 중심
- 단비는 **스키마 + 빠른 검색 + 합성**이 한 앱에 묶여 있어 카파시의 "compounding notes"가 한 줄 명령으로 실현됨

### 3.5. 🔗 [[Wiki 링크]] + 백링크

**시나리오**
> `ui.md`에 `"로그인 흐름 [[보니/backend.md]] 참고"` 적으면 클릭 가능한 링크가 됨. `backend.md` 열면 하단에 **"이 문서를 참조하는 곳"** 패널에 `ui.md`가 표시됨.

**기술**
- Rust가 모든 `.md` 파싱 → `[[프로젝트/도메인.md]]` 추출 → incoming/outgoing 양방향 인덱스
- 파일 변경 감지되면 watcher가 인덱스 자동 갱신
- Writer 모델도 자연스럽게 wiki 링크 생성하도록 시스템 프롬프트 지시

### 3.6. 🛡️ Git 기반 자동 Undo

**시나리오**
> AI가 plan.md 섹션 하나를 새 내용으로 덮어씀. "어 이전 내용이 더 좋았는데?" → 채팅 카드 옆 **되돌리기** 버튼 한 번. 이전 상태 복원.

**기술**
- Vault = git repo (앱 최초 실행 시 자동 `git init`)
- **매 수정 전/후 2번 자동 커밋** — "pre-edit snapshot" + "after-edit"
- `undo_last` = `git reset --hard HEAD^`
- `log.md` 자동 타임라인, `history.jsonl` 구조화 이벤트 스트림

**왜 이게 중요한가**
- AI 편집에 대한 공포: "망쳐놓으면 어떡하지?" → **항상 되돌릴 수 있음**이 전제
- Obsidian Sync는 유료, 별도 플러그인 설치 필요
- 단비는 **기본값으로 제공**

### 3.7. 🏥 Self-healing Vault

**시나리오**
> 홈 화면 열면 "정리 제안" 섹션에: `보니/notes-old.md` 는 ORPHAN (아무도 참조 안 함), `상식이/backend.md` 는 LARGE (60KB 초과, 분할 권장). 클릭으로 이동.

**기술 (현재 MVP)**
- 로컬 Rust 스캐너 — Orphan(고립) / Empty(빈 파일) / EmptyProject / Oversized 감지
- 매일 홈 화면 진입 시 자동 스캔
- **향후 확장**: LLM 기반 의미 중복 감지 ("이 3개 문서에 같은 API 스펙이 있어요")

---

## 4. 경쟁 도구와의 비교

### 기능 매트릭스

| 기능 | 단비 | Obsidian | Notion | Raycast | Alfred |
|---|---|---|---|---|---|
| 로컬 마크다운 | ✅ | ✅ | ❌ | ❌ | ❌ |
| AI 문서 편집 | ✅ 내장 | 🟡 플러그인 | ✅ 유료 | ✅ 유료 | ❌ |
| **AI 라우팅** (지시→파일) | ✅ | ❌ | ❌ | ❌ | ❌ |
| 전역 핫키 | ✅ | ❌ | ❌ | ✅ | ✅ |
| 백그라운드 실행 | ✅ 트레이 | ❌ | ❌ | ✅ | ✅ |
| **PDF ingestion → 분산** | ✅ | 🟡 플러그인 | 🟡 단일 페이지 | ❌ | ❌ |
| **교차 문서 합성** | ✅ Compound | ❌ | ❌ | ❌ | ❌ |
| Wiki 링크 + 백링크 | ✅ | ✅ | 🟡 | ❌ | ❌ |
| Git 자동 커밋 / Undo | ✅ | 🟡 플러그인 | ❌ | ❌ | ❌ |
| Self-healing 제안 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 한국어 검색 (ngram) | ✅ tantivy | 🟡 약함 | ✅ | ✅ | 🟡 |
| BYOK (비용 투명) | ✅ | ✅ | ❌ 구독 | ❌ 구독 | ❌ |
| 데이터 주권 | ✅ 완전 로컬 | ✅ | ❌ 클라우드 | ❌ | ❌ |

### 핵심 비교 한 문장씩

- **vs Obsidian**: "Obsidian은 내가 쓰는 도구, 단비는 나 대신 정리해주는 비서"
- **vs Notion**: "Notion은 클라우드 워크스페이스, 단비는 로컬 에이전트"
- **vs Raycast/Alfred**: "Raycast는 어디서나 명령, 단비는 어디서나 **기록**"
- **vs Cursor에 붙이는 AI 메모**: "Cursor는 코드 중심, 단비는 결정·지식 중심 + 다중 프로젝트"

---

## 5. 카파시 LLM Wiki 방법론과의 관계

Andrej Karpathy가 제시한 개인 지식 관리 원칙 4가지를 단비가 그대로 구현:

| 카파시 원칙 | 단비 구현 |
|---|---|
| plain markdown, 파일 단위 격리 | `Projects/<proj>/<domain>.md` 강제 |
| 주제 중심 폴더 | 프로젝트 × 도메인 스키마 |
| LLM이 파일 통째로 읽게 | 라우팅/플래닝 시 `current_document` 전체 전달 |
| 즉시 적기 (outsource memory) | **Quick Capture** |
| 쓰기를 통한 사고 (compound over time) | **Compound intent** |
| "어디 적었더라?" 검색 | **3단 검색** |
| git으로 버전 관리 | **vault = git repo, 자동 커밋** |

단비의 정체성 한 줄: **"Karpathy's LLM wiki, as an app."**

---

## 6. 비용 모델

사용자 AWS Bedrock 키로 직접 호출하므로 단비 자체는 무료. LLM 호출 비용만 본인 부담.

### 호출 모델 구분

| 기능 | 모델 | 호출 빈도 | 호출당 비용 (예상) |
|---|---|---|---|
| 채팅 라우팅 | Haiku 4.5 | 매 메시지 | ~$0.001 |
| 채팅 플래닝 | Sonnet 4.6 | 매 메시지 | ~$0.01 |
| Quick Capture | Haiku + Sonnet | 매 캡처 | ~$0.011 |
| 검색 Tier 1·2 | — | 매 키 입력 | **$0** |
| 검색 Tier 3 (AI 재정렬) | Haiku | 버튼 누를 때만 | ~$0.002 |
| **Compound** | Sonnet | 버튼 누를 때만 | ~$0.03 ~ $0.15 |
| Self-healing 스캔 | — | 매 홈 진입 | **$0** |

### 실사용 예상 (월)
- 하루 20번 캡처 + 3번 Compound = 월 약 **$5~15**
- Notion AI 구독은 월 $10, Raycast Pro는 월 $8
- BYOK의 장점: **내가 AWS 콘솔에서 실시간 확인 가능**, 쓴 만큼만

### 모델 설정
- 설정 → LLM 섹션에서 Routing/Writer 모델 각각 자유 선택
- 저렴한 Haiku로 둘 다 돌리면 월 $2 이하도 가능

---

### 3.8. 🔌 MCP 서버 (Claude Code / Cursor 연결)

**시나리오**
> Claude Code에서 `/mcp` 로 단비를 연결 → "오늘 작업한 내용 단비 보니/daily에 정리해줘" 한 마디 → Claude Code가 `danbi_log` tool call → 세션 끝나도 **대화가 마크다운으로 남음**. 다음 날 `danbi_search` 로 "어제 토큰 만료 어떻게 하기로 했지?" 물으면 Claude가 vault에서 찾아서 답.

**제공 tool 6가지**
| tool | 설명 | LLM 호출 |
|---|---|---|
| `danbi_list_projects` | 프로젝트/도메인 트리 | 0원 |
| `danbi_search` | tantivy 전문 검색 | 0원 |
| `danbi_read` | 특정 파일 내용 | 0원 |
| `danbi_log` | 오늘 daily/ 에 append | 0원 |
| `danbi_append` | 특정 파일에 append (+자동 git) | 0원 |
| `danbi_recent` | 최근 수정 목록 | 0원 |

**설정**
1. 단비 설정 → **MCP 섹션** → 켜기
2. 생성된 `{url, token}` 복사
3. Claude Code의 `~/.claude/mcp.json` 에 붙여넣기:
```json
{
  "mcpServers": {
    "danbi": {
      "url": "http://127.0.0.1:47921/mcp",
      "headers": { "Authorization": "Bearer ..." }
    }
  }
}
```

**보안**
- `127.0.0.1`만 바인딩 — 외부 네트워크에서 접근 불가
- 요청마다 Bearer 토큰 검증
- 토큰 회전 버튼으로 언제든 갱신

**왜 이게 결정적인가**
- Claude Code 세션은 끝나면 **증발** — 지식 축적 불가
- 단비 MCP 붙이면 AI가 자동으로 작업 일지 남김
- 다음 세션의 Claude가 **어제의 나를 기억**

**자세한 사용법**: [docs/MCP_GUIDE.md](docs/MCP_GUIDE.md) — Claude Code, Cursor, curl 예시, 실전 워크플로, 문제 해결까지.

---

## 부록. 아직 없지만 곧 오는 것

| 기능 | 설명 |
|---|---|
| 로컬 embedding 검색 | 의미 검색도 LLM 없이 빠르게 |
| 주기적 Self-healing 제안 | "이 3개 문서에 같은 스펙이 있어요" |
| 팀 모드 | 여러 사람이 같은 vault 공동 편집 |

---

**© 2026 · 단비는 한국 AI 빌더 커뮤니티의 피드백으로 진화합니다.**
